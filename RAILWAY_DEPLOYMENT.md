# 🚂 Railway Deployment Guide - PokePao

Полное пошаговое руководство по развертыванию приложения PokePao на Railway.

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Создание проекта на Railway](#создание-проекта-на-railway)
3. [Настройка базы данных](#настройка-базы-данных)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [Развертывание приложения](#развертывание-приложения)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Troubleshooting](#troubleshooting)

---

## Подготовка

### Что у вас должно быть:

- ✅ Аккаунт на [Railway.app](https://railway.app)
- ✅ GitHub репозиторий с вашим кодом
- ✅ Railway CLI (опционально, но рекомендуется)

### Установка Railway CLI (опционально):

```bash
npm install -g @railway/cli
railway login
```

---

## Создание проекта на Railway

### Шаг 1: Создайте новый проект

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий с PokePao

---

## Настройка базы данных

### Шаг 2: Добавьте PostgreSQL

1. В вашем Railway проекте нажмите **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных и добавит переменную окружения `DATABASE_URL`
3. Подождите ~30 секунд пока база данных полностью развернется

✅ После создания БД, переменная `DATABASE_URL` автоматически доступна во всех сервисах проекта!

---

## Настройка переменных окружения

### Шаг 3: Добавьте обязательные переменные

Перейдите в **Variables** вашего веб-сервиса и добавьте:

#### 🔴 Обязательные переменные:

```bash
NODE_ENV=production
SESSION_SECRET=ваш-секретный-ключ-минимум-32-символа-измените-это
```

> ⚠️ **ВАЖНО**: `SESSION_SECRET` должен быть случайной строкой минимум 32 символа!
> 
> Сгенерировать можно так:
> ```bash
> node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
> ```

#### 🟢 Опциональные переменные:

```bash
# Telegram уведомления (если нужны)
TELEGRAM_BOT_TOKEN=ваш-токен
TELEGRAM_CHAT_ID=ваш-chat-id

# Загрузка файлов (если используете Volume)
UPLOAD_DIR=/data/uploads
```

---

## Развертывание приложения

### Шаг 4: Настройка Volume для файлов (опционально)

Если ваше приложение использует загрузку изображений:

1. Нажмите **"New"** → **"Volume"**
2. Имя: `uploads`
3. Mount path: `/data`
4. Attach to: ваш веб-сервис

### Шаг 5: Проверка конфигурации деплоя

Railway автоматически использует `railway.json`:

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run railway:start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

✅ **Это уже настроено в проекте!** Railway автоматически:
- Установит зависимости и соберет фронтенд
- Запустит `npm run railway:start`, который:
  - Создаст все таблицы БД из схемы (drizzle-kit push)
  - Заполнит начальными данными (seed)
  - Создаст admin пользователя
  - Запустит production сервер

### Шаг 6: Запустите деплой

#### Вариант A: Автоматический деплой (рекомендуется)

Railway автоматически разворачивает проект при создании. Просто подождите завершения деплоя (~2-5 минут).

#### Вариант B: Через GitHub

Любой push в main/master ветку автоматически запустит новый деплой.

#### Вариант C: Через Railway CLI

```bash
railway link  # Привязать к проекту (один раз)
railway up    # Развернуть
```

---

## Проверка работоспособности

### Шаг 7: Проверьте что всё работает

#### 1. Проверьте логи деплоя

Откройте **Deployments** → последний деплой → **View Logs**

Вы должны увидеть:
```
🚀 Starting Database Initialization for Railway...
📡 Step 1: Testing database connection...
✅ Database connection successful!
🏗️  Step 3: Creating database schema...
✅ Database schema created successfully!
🌱 Step 4: Seeding database...
✅ Database seeded successfully!
👤 Step 5: Creating admin user...
✅ Admin user created successfully!
✨ Database initialization completed successfully!
```

#### 2. Проверьте healthcheck endpoint

Откройте в браузере: `https://ваш-домен.up.railway.app/api/health`

Вы должны увидеть:
```json
{
  "status": "healthy",
  "database": "connected",
  "timestamp": "2025-11-21T...",
  "environment": "production"
}
```

#### 3. Проверьте сайт

Откройте: `https://ваш-домен.up.railway.app`

Должна загрузиться главная страница с меню.

#### 4. Проверьте админ-панель

1. Откройте: `https://ваш-домен.up.railway.app/admin`
2. Войдите с учетными данными:
   - **Username**: `admin`
   - **Password**: `mk509918`

✅ Вы должны увидеть админ-панель с категориями, меню, ингредиентами и т.д.

#### 5. Проверьте все функции

Протестируйте следующие функции:

- ✅ **Главное меню** - отображаются все категории и товары
- ✅ **Wunsch Bowl** - конструктор работает, все ингредиенты загружаются
- ✅ **Галерея** - загружается корректно
- ✅ **Админка → Меню** - можно редактировать товары
- ✅ **Админка → Ингредиенты** - все типы ингредиентов присутствуют:
  - Protein
  - Base
  - Marinade
  - Fresh
  - Sauce
  - Topping
  - Extra Protein
  - Extra Fresh
  - Extra Sauce
  - Extra Topping
- ✅ **Админка → Product Variants** - можно создавать варианты
- ✅ **Админка → Snapshots** - система снапшотов работает
- ✅ **Админка → Gallery** - можно загружать изображения
- ✅ **Админка → Settings** - режим обслуживания переключается

### Шаг 8: Верификация базы данных (опционально)

Вы можете запустить скрипт верификации через Railway CLI:

```bash
railway run npm run db:verify
```

Это выведет детальный отчет о состоянии БД:
- Все таблицы и количество записей
- Типы ингредиентов и их количество
- Статистику по menu items
- И многое другое

---

## Troubleshooting

### Проблема: База данных не инициализируется

**Симптомы**: В логах ошибки "DATABASE_URL not set" или "Failed to connect"

**Решение**:
1. Убедитесь что PostgreSQL сервис создан и запущен
2. Проверьте что оба сервиса (веб + БД) в одном проекте Railway
3. Перезапустите деплой

### Проблема: Сайт возвращает 500 ошибки на API endpoints

**Симптомы**: `/api/menu-items`, `/api/ingredients` возвращают 500

**Решение**:
1. Проверьте логи Railway: `railway logs --follow`
2. Проверьте что инициализация БД прошла успешно (см. логи выше)
3. Попробуйте запустить инициализацию вручную:
   ```bash
   railway run npm run db:init
   ```

### Проблема: Админ-панель не работает / не могу войти

**Симптомы**: Ошибка "Invalid credentials" или белая страница

**Решение**:
1. Проверьте что `SESSION_SECRET` установлен в Variables
2. Пересоздайте admin пользователя:
   ```bash
   railway run npm run db:create-admin
   ```
3. Проверьте логи на ошибки авторизации

### Проблема: Ингредиенты не отображаются в Wunsch Bowl

**Симптомы**: Конструктор пустой, нет опций выбора

**Решение**:
1. Проверьте что seed данные загрузились:
   ```bash
   railway run npm run db:verify
   ```
2. Если данных нет, запустите seed вручную:
   ```bash
   railway run npm run db:seed
   ```
3. Проверьте типы ингредиентов в админке

### Проблема: Галерея / загрузка файлов не работает

**Симптомы**: Ошибки при загрузке изображений

**Решение**:
1. Убедитесь что создан Volume `/data`
2. Проверьте что установлена переменная `UPLOAD_DIR=/data/uploads`
3. Проверьте права доступа в логах

### Проблема: Снапшоты не создаются

**Симптомы**: Ошибка при создании snapshot

**Решение**:
1. Проверьте что таблицы `snapshot_*` созданы:
   ```bash
   railway run npm run db:verify
   ```
2. Если таблиц нет, пересоздайте схему:
   ```bash
   railway run npm run db:init
   ```

### Проблема: Product Variants не работают

**Симптомы**: Не отображаются варианты товаров

**Решение**:
1. Проверьте что таблица `product_variants` создана
2. Создайте варианты в админке: Menu Items → выберите товар → Manage Variants

---

## Полезные команды

### Через Railway CLI:

```bash
# Просмотр логов в реальном времени
railway logs --follow

# Запуск команд в production окружении
railway run <команда>

# Примеры:
railway run npm run db:verify        # Проверить БД
railway run npm run db:init          # Переинициализировать БД
railway run npm run db:create-admin  # Пересоздать админа
```

### Проверка таблиц напрямую:

```bash
# Подключиться к PostgreSQL
railway run psql $DATABASE_URL

# Примеры SQL запросов:
SELECT COUNT(*) FROM categories;
SELECT COUNT(*) FROM ingredients;
SELECT type, COUNT(*) FROM ingredients GROUP BY type;
SELECT * FROM admin_users;
```

---

## Обновление приложения

### Автоматическое обновление:

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Update feature X"
   git push
   ```
3. Railway автоматически пересоберет и задеплоит

### Ручное обновление через CLI:

```bash
railway up
```

---

## Важные заметки

### 🔴 Безопасность:

1. **Обязательно** измените пароль админа после первого входа!
2. **Обязательно** используйте сильный `SESSION_SECRET`
3. Не коммитьте секреты в Git

### 🟡 Performance:

- Railway автоматически масштабирует ваше приложение
- Используйте Volume для persistent storage файлов
- База данных оптимизирована для Railway (connection pooling, SSL)

### 🟢 Мониторинг:

- Используйте healthcheck endpoint: `/api/health`
- Следите за логами через Railway Dashboard
- Настройте Telegram уведомления для заказов (опционально)

---

## Поддержка

Если возникли проблемы:

1. ✅ Проверьте логи: `railway logs --follow`
2. ✅ Запустите верификацию: `railway run npm run db:verify`
3. ✅ Проверьте healthcheck: `https://ваш-домен.up.railway.app/api/health`
4. ✅ Посмотрите Railway Fixes Guide: `RAILWAY_FIXES_GUIDE.md`

---

## Версия

Последнее обновление: 21 ноября 2025

Эта инструкция соответствует новой системе инициализации БД через `server/init-database.ts`.


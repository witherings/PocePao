# Руководство по исправлению проблем на Railway

## Обнаруженные и исправленные проблемы

### 1. **Снапшоты не работают** ✅
**Проблема:** Таблица `snapshot_ingredients` отсутствует в базе данных
**Решение:** Обновлен `server/migrate.ts` для создания недостающей таблицы

### 2. **Extra Zutaten не отображаются** ✅
**Проблема:** Ингредиенты создавались с неправильным типом "extra" вместо "extra_protein", "extra_fresh", "extra_sauce", "extra_topping"
**Решение:** Исправлен `client/src/pages/AdminMenu.tsx` для правильного маппинга типов ингредиентов

### 3. **Галерея не работает** ✅
**Проблема:** Таблица `gallery_images` должна быть создана через миграции
**Решение:** Миграция уже включает создание этой таблицы

### 4. **Product Variants пустые** ✅
**Проблема:** Таблица существует, но может быть пустой или варианты не созданы
**Решение:** Миграция проверяет и создает таблицу, варианты нужно создать в админке

### 5. **Неправильные цены** ✅
**Проблема:** Отображение цен для extra ингредиентов и протеинов
**Решение:** Исправлен маппинг типов, что должно решить проблему с ценами

## 🚨 ВАЖНО: ПОРЯДОК РАЗВЕРТЫВАНИЯ

**⚠️ ОБЯЗАТЕЛЬНО СЛЕДУЙТЕ ЭТОМУ ПОРЯДКУ:**

1. **Создайте backup базы данных** (через Railway Dashboard)
2. **Сначала тестируйте на staging/копии БД** 
3. **Только после проверки - применяйте к production**

## Шаги для развертывания на Railway (STAGING FIRST!)

### Шаг 0: Создайте backup (КРИТИЧНО!)

**⚠️ НЕ ПРОПУСКАЙТЕ ЭТОТ ШАГ!**

1. Откройте Railway Dashboard
2. Перейдите к вашему Postgres сервису
3. Выберите "Backups" → "Create Backup"
4. Дождитесь завершения создания backup
5. Запишите время создания backup для возможного восстановления

### Шаг 1: Разверните обновленный код на Railway

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Fix snapshot_ingredients table and extra ingredient types"
git push
```

2. Railway автоматически начнет новый деплой

### Шаг 2: Запустите миграцию на Railway

⚠️ **РЕКОМЕНДАЦИЯ:** Если возможно, сначала создайте staging окружение с копией БД и тестируйте там

После успешного деплоя, подключитесь к Railway и выполните миграцию:

**Вариант А: Через Railway CLI**
```bash
railway run npm run db:migrate
```

**Вариант Б: Через Railway Dashboard**
1. Откройте ваш проект на Railway
2. Перейдите в раздел "Deployments"
3. Нажмите на активный деплой
4. Откройте терминал (Terminal)
5. Выполните команду:
```bash
npm run db:migrate
```

### Шаг 3: Автоматически исправьте существующие "extra" ингредиенты

Запустите скрипт для автоматического обновления неправильных "extra" ингредиентов:

```bash
railway run npm run db:fix-extras
```

**Как работает скрипт:**
1. Находит все ингредиенты с типом "extra"
2. Для каждого пытается найти базовый ингредиент с таким же названием
3. Если найден - использует тип базового ингредиента (matched_by_name)
4. Если не найден - определяет тип по ключевым словам в названии (inferred_from_keywords)
5. Если не удалось определить - устанавливает extra_fresh по умолчанию (defaulted_to_fresh)
6. Обновляет запись в базе данных с правильным типом
7. Выводит детальный отчет о каждой категории

**Важно - ОБЯЗАТЕЛЬНАЯ ПРОВЕРКА:**
- ⚠️ Скрипт НЕ удаляет данные, но может установить неправильный тип для некоторых ингредиентов
- ⚠️ После выполнения скрипт покажет список ингредиентов, которым установлен тип extra_fresh по умолчанию
- ✅ ЭТИ ИНГРЕДИЕНТЫ ТРЕБУЮТ РУЧНОЙ ПРОВЕРКИ в админ панели
- ✅ Проверьте категории: Extra Protein, Extra Frische Zutaten, Extra Sauce, Extra Toppings
- ✅ Убедитесь, что ингредиенты находятся в правильных категориях

### Шаг 4: Создайте Product Variants в админке

Для Poke Bowls с вариантами (например, Fritz Cola с выбором вкуса):

1. Зайдите в админку
2. Откройте меню (Menu Items)
3. Найдите товар, для которого нужны варианты (например, Fritz Cola)
4. Нажмите кнопку "Manage Variants"
5. Включите "Has Variants"
6. Выберите тип варианта: "base" или "flavor"
7. Добавьте варианты (например, для Fritz Cola: "Apfel", "Orange", "Zitrone", и т.д.)
8. Сохраните

### Шаг 5: ОБЯЗАТЕЛЬНАЯ ВЕРИФИКАЦИЯ ⚠️

После выполнения всех шагов ОБЯЗАТЕЛЬНО проверьте следующее:

**1. Проверка Extra Ингредиентов (КРИТИЧНО)**
```sql
-- Подключитесь к Railway Database и выполните:

-- Проверьте количество ингредиентов по типам
SELECT type, COUNT(*) as count 
FROM ingredients 
WHERE type LIKE 'extra_%' 
GROUP BY type;

-- Проверьте, не осталось ли неправильных "extra"
SELECT COUNT(*) FROM ingredients WHERE type = 'extra';
-- Должно быть 0
```

**2. Проверка в Админ Панели**
- Зайдите в Админ Панель → Zutaten (Ingredients)
- Фильтруйте по каждому типу extra:
  - ✅ Extra Protein - проверьте, что там только протеины
  - ✅ Extra Frische - проверьте, что там только свежие ингредиенты
  - ✅ Extra Sauce - проверьте, что там только соусы
  - ✅ Extra Toppings - проверьте, что там только топпинги
- Если найдете ингредиенты в неправильной категории:
  1. Откройте ингредиент для редактирования
  2. Измените тип на правильный
  3. Сохраните

**3. Проверка Wunsch Bowl Constructor**
- Откройте сайт и создайте Wunsch Bowl
- На шаге "Extras" проверьте, что отображаются:
  - Extra Protein (с протеинами и ценами)
  - extra frische Zutaten (со свежими ингредиентами)
  - extra Sauce (с соусами)
  - extra Toppings (с топпингами)

**4. Проверка Снапшотов**
- Зайдите в админку → Content Snapshots
- Создайте тестовый снапшот
- Убедитесь, что создается без ошибок
- Удалите тестовый снапшот

**5. Проверка Галереи**
- Зайдите в админку → Galerie
- Проверьте, что Hauptgalerie отображается
- Загрузите тестовое изображение
- Проверьте, что оно отображается на сайте

**6. Проверка Product Variants**
- Если у вас есть товары с вариантами (Fritz Cola, и т.д.)
- Проверьте, что варианты отображаются при добавлении в корзину
- Убедитесь, что требуется выбор варианта перед добавлением

## Список измененных файлов

1. **server/migrate.ts** - Добавлено создание таблицы `snapshot_ingredients`
2. **client/src/pages/AdminMenu.tsx** - Исправлен маппинг типов extra ингредиентов
3. **server/fix-extra-ingredients.ts** - Новый скрипт для очистки неправильных extra ингредиентов
4. **package.json** - Добавлен скрипт `db:fix-extras`

## Команды для Railway

```bash
# Запустить миграцию
railway run npm run db:migrate

# Исправить extra ингредиенты
railway run npm run db:fix-extras

# Пересоздать admin пользователя (если нужно)
railway run npm run db:create-admin

# Проверить подключение к БД
railway run npm run db:test
```

## Важные замечания

🔴 **КРИТИЧНО - Backup**: 
- **ОБЯЗАТЕЛЬНО** создайте backup через Railway Dashboard ПЕРЕД ЛЮБЫМИ изменениями
- Проверьте, что backup успешно создан
- Запишите время создания на случай восстановления

⚠️ **Staging-First подход**: 
- **НАСТОЯТЕЛЬНО РЕКОМЕНДУЕТСЯ** создать staging окружение с копией production БД
- Выполните ВСЕ шаги на staging
- Проверьте результаты с помощью раздела "Шаг 5: ОБЯЗАТЕЛЬНАЯ ВЕРИФИКАЦИЯ"
- Только после успешного тестирования - применяйте к production

⚠️ **Extra ингредиенты**: 
- Скрипт `db:fix-extras` автоматически обновляет типы БЕЗ удаления данных
- Обрабатывает NULL значения (пропускает их с предупреждением)
- НО может установить неправильный тип для ингредиентов без явного совпадения
- ОБЯЗАТЕЛЬНА ручная проверка результатов (см. раздел "Шаг 5: ОБЯЗАТЕЛЬНАЯ ВЕРИФИКАЦИЯ")

⚠️ **Product Variants**: Варианты продуктов создаются только через админку, автоматическое создание не предусмотрено

## Поддержка

Если после выполнения всех шагов проблемы остаются:

1. Проверьте логи Railway: `railway logs`
2. Проверьте состояние базы данных
3. Убедитесь, что все миграции применены успешно
4. Проверьте переменные окружения DATABASE_URL

## Версия

Эти исправления применены: 21 ноября 2025

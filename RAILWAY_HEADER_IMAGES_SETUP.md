# Настройка изображений хедера на Railway

## Проблема
Фотографии в хедере (карусель) не отображаются на Railway, потому что они сохраняются в эфемерной файловой системе, которая очищается при каждом перезапуске контейнера.

## Решение
Настроить постоянное хранилище (Volume) на Railway, чтобы все загруженные изображения сохранялись между перезапусками.

---

## Шаг 1: Создать Volume на Railway

1. Зайди в **Railway Dashboard** → Выбери свой проект
2. В левом меню найди **"Volumes"** (или в правом верхнем углу нажми **"New"** → **"Volume"**)
3. Нажми **"New Volume"**
4. Настрой Volume:
   - **Name**: `uploads` (или любое другое имя)
   - **Mount Path**: `/data` (ОБЯЗАТЕЛЬНО именно `/data`)
   - **Size**: `1 GB` (можно больше, если нужно)
5. Нажми **"Create"**
6. Подключи Volume к своему **web service** (если не подключился автоматически)

---

## Шаг 2: Установить переменную окружения

1. В Railway Dashboard открой свой проект
2. Перейди на вкладку **"Variables"**
3. Нажми **"New Variable"**
4. Добавь переменную:
   - **Key**: `UPLOAD_DIR`
   - **Value**: `/data/uploads`
5. Нажми **"Add"**

---

## Шаг 3: Передеплой проект

1. После добавления Volume и переменной окружения Railway автоматически начнет передеплой
2. Если нет - нажми **"Deploy"** или сделай любой коммит в репозиторий
3. Дождись завершения деплоя (обычно 2-5 минут)

---

## Шаг 4: Загрузить фотографии для хедера

1. Открой свой сайт на Railway
2. Войди в **админ-панель**: `https://твой-сайт.railway.app/admin`
3. Перейди в раздел **"Content"** (Контент) → **"Startseite"** (Главная страница)
4. В разделе **"Header Images"** (Изображения хедера):
   - Нажми **"Upload Image"** или **"Загрузить изображение"**
   - Выбери **3 фотографии** для карусели
   - Загрузи их по очереди
5. После загрузки фотографии должны появиться в списке

---

## Шаг 5: Проверить что всё работает

1. Открой главную страницу сайта
2. Должна отображаться карусель с тремя фотографиями
3. Карусель автоматически переключается каждые 5 секунд
4. **Тест на постоянство**: 
   - Перезапусти проект на Railway (Settings → "Restart")
   - После перезапуска открой сайт снова
   - Фотографии должны остаться на месте

---

## Что изменилось в коде

Я внес следующие изменения для поддержки Railway:

### 1. Изменен путь сохранения page-images

**Было** (в `server/routes.ts`):
- Файлы сохранялись в `public/images/pages/`
- URL: `/images/pages/startseite/photo.jpg`
- ❌ Проблема: эти файлы исчезают при перезапуске на Railway

**Стало**:
- Файлы сохраняются в `uploadDir/pages/` (т.е. `/data/uploads/pages/` на Railway)
- URL: `/uploads/pages/startseite/photo.jpg`
- ✅ Файлы сохраняются в постоянном Volume

### 2. Обновлен DELETE endpoint

Теперь поддерживает оба формата URL (для обратной совместимости):
- Старый формат: `/images/pages/...`
- Новый формат: `/uploads/pages/...`

---

## Технические детали

### Как это работает

1. **Development (Replit)**:
   - `UPLOAD_DIR` не установлен
   - Файлы сохраняются в `uploads/` (локальная директория)

2. **Production (Railway)**:
   - `UPLOAD_DIR=/data/uploads` (ты установишь вручную)
   - `/data` это mount point для Volume
   - Файлы сохраняются в `/data/uploads/pages/startseite/`
   - Volume сохраняет данные между перезапусками

### Автоматическая fallback логика

Если на Railway еще нет загруженных фотографий, карусель автоматически покажет красивые placeholder-изображения с Unsplash (пока ты не загрузишь свои фотографии).

---

## Часто задаваемые вопросы

### Вопрос: Нужно ли удалять старые изображения из базы данных?
**Ответ**: Нет, не нужно. Код поддерживает оба формата URL. Старые записи будут работать, а новые будут создаваться с новым форматом.

### Вопрос: Что делать если фотографии всё равно исчезают?
**Ответ**: Проверь что:
1. Volume создан и подключен к web service
2. Mount path = `/data` (именно так)
3. Переменная `UPLOAD_DIR=/data/uploads` установлена
4. Проект передеплоен после этих изменений

### Вопрос: Сколько фотографий можно загрузить?
**Ответ**: Для хедера рекомендуется 3 фотографии (карусель выглядит лучше всего). Но технически можно загрузить сколько угодно.

### Вопрос: Какой размер/формат фотографий лучше?
**Ответ**: 
- Формат: JPG или PNG
- Размер: 1920x1080 или больше (Full HD)
- Соотношение сторон: 16:9 (горизонтальные фото)
- Вес: до 2-3 MB каждая (для быстрой загрузки)

---

## Если что-то не работает

1. Проверь Railway логи:
   ```
   Railway Dashboard → Deployments → View Logs
   ```

2. Убедись что Volume подключен:
   ```
   Railway Dashboard → Volumes → Check Status
   ```

3. Проверь переменные окружения:
   ```
   Railway Dashboard → Variables → UPLOAD_DIR должна быть /data/uploads
   ```

4. Передеплой проект:
   ```
   Railway Dashboard → Settings → Redeploy
   ```

---

## Готово! 🎉

После выполнения всех шагов:
- ✅ Volume настроен и работает
- ✅ Изображения сохраняются навсегда
- ✅ Карусель в хедере показывает твои фотографии
- ✅ Всё работает даже после перезапуска Railway

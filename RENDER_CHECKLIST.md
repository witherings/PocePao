# ✅ Чеклист для деплоя на Render

## Что сделано в коде (автоматически):

✅ **package.json исправлен**
   - `build` команда теперь только `vite build` (БЕЗ drizzle-kit push)
   - Добавлена команда `render:setup` для первого деплоя

✅ **seed.ts сделан безопасным**
   - Проверяет наличие данных перед seed
   - НЕ удаляет существующие данные
   - Безопасен для production

---

## Что нужно сделать в Render Dashboard:

### 1. 📝 Обновите Build Command
**Было (неправильно):**
```bash
drizzle-kit push && vite build
```

**Должно быть:**
```bash
npm run build
```

### 2. 🚀 Обновите Start Command

**Для первого деплоя (установка всего):**
```bash
npm run render:setup && npm run start
```

**Для последующих деплоев (рекомендуется):**
```bash
npm run db:push; npm run start
```

### 3. 🔐 Проверьте переменные окружения

В вашем скриншоте вижу `ADMIN_USER`, но код использует `ADMIN_USERNAME`.

**Нужно переименовать:**
- `ADMIN_USER` → `ADMIN_USERNAME` (или наоборот, смотря что использует код)

**Обязательные переменные:**
- ✅ `DATABASE_URL` (уже есть)
- ✅ `SESSION_SECRET` (уже есть)
- ✅ `ADMIN_PASSWORD` (уже есть - mk509918)
- ⚠️ `ADMIN_USERNAME` или `ADMIN_USER` (проверьте название!)

### 4. 🔄 Manual Deploy

1. Нажмите **"Manual Deploy"** → **"Clear build cache & deploy"**
2. Дождитесь завершения (3-5 минут)
3. Проверьте логи

---

## 📊 Что увидите в логах при успехе:

```
> npm run render:setup

> drizzle-kit push
✅ Changes applied

> tsx server/seed.ts
🌱 Starting database seeding...
📝 Database is empty. Proceeding with seed...
✅ Created 6 categories
✅ Created 20 menu items
✅ Created 50 ingredients

> tsx server/create-admin.ts
✅ Admin user created successfully!
   Username: admin
   
> npm run start
🚀 Server started on port 5000
```

---

## ⚠️ Возможные ошибки и решения:

### "ECONNREFUSED" - База недоступна
**Решение**: Проверьте что PostgreSQL база создана и активна в Render

### "Database already has data. Skipping seed"
**Это нормально!** Данные уже есть, seed пропущен для безопасности.

### "Admin user already exists"
**Это нормально!** Админ уже создан.

### "ADMIN_PASSWORD not set"
**Решение**: Проверьте что переменная `ADMIN_PASSWORD` установлена в Environment Variables

### "Cannot find module"
**Решение**: Очистите build cache и пересоберите

---

## 🎯 Итоговая конфигурация:

| Параметр | Значение |
|----------|----------|
| **Build Command** | `npm run build` |
| **Start Command** | `npm run db:push; npm run start` |
| **Node Version** | 20.x (из package.json engines) |
| **Environment** | Production |

---

## 📞 После успешного деплоя:

1. Сайт доступен на вашем Render URL
2. Админка: `https://your-app.onrender.com/admin/login`
3. Логин: `admin`
4. Пароль: `mk509918`

---

**Дата**: 13.11.2025
**Статус**: Готово к деплою ✅

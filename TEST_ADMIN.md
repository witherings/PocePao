# 🔐 Тестирование Админ Панели

## Данные для входа:
- **Имя пользователя**: `admin`
- **Пароль**: `mk509918`

## Как протестировать:

### 1. Вход в админ панель
1. Откройте страницу `/admin` или `/admin/login`
2. Введите логин: `admin`
3. Введите пароль: `mk509918`
4. Нажмите кнопку "Anmelden" (Войти)
5. Вы должны быть перенаправлены на `/admin/dashboard`

### 2. Доступные страницы администратора:
- `/admin/dashboard` - Панель управления
- `/admin/snapshots` - Управление снимками (резервные копии)
- `/admin/gallery` - Управление галереей
- `/admin/menu` - Управление меню
- `/admin/content` - Управление контентом

### 3. Альтернативный вход (только для разработки):
В режиме разработки (NODE_ENV=development) вы автоматически войдете в систему
при обращении к любому `/api/admin/*` endpoint.

## Проверка работы:

### Тест входа через curl:
```bash
curl -X POST http://localhost:5000/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"mk509918"}' \
  -c cookies.txt -v
```

Должен вернуть:
```json
{"success":true,"user":{"id":"...","username":"admin"}}
```

### Проверка сессии:
```bash
curl http://localhost:5000/api/admin/me \
  -b cookies.txt
```

Должен вернуть информацию о пользователе, если вход выполнен.

## Возможные проблемы:

### "Invalid credentials" (Неверные учетные данные)
- Проверьте, что логин точно `admin` (маленькими буквами)
- Проверьте, что пароль точно `mk509918` (без пробелов)

### Не загружается страница
- Убедитесь, что сервер запущен на порту 5000
- Проверьте логи сервера в консоли

### Сессия не сохраняется
- Очистите cookies браузера
- Убедитесь, что браузер принимает cookies

## База данных

Админ пользователь создан в базе данных PostgreSQL.
Проверить можно командой:
```bash
npm run db:studio
```

---

**Примечание**: В production режиме автоматический вход НЕ работает.

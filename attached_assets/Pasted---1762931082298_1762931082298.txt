Привет! У меня есть задача по миграции и объединению двух проектов. Пожалуйста, внимательно прочти этот контекст, прежде чем мы приступим к плану.

Ситуация:
1_OLD_SITE_BACKEND: Это старый PHP/MySQL сайт. В нем ценная "внутрянка":

Работающий Telegram-бот для бронирования столов.

Админ-панель (/www/admin) для управления меню (блюда, категории, цены) и другим контентом (галерея, SEO).

База данных (.sql) со всем контентом.

2_NEW_SITE_DESIGN: Это мой новый, готовый сайт на TypeScript/Next.js.

В нем уже есть весь Frontend: страницы меню, готовый компонент галереи, готовый компонент корзины и страница оформления заказа.

Проблема: Он "мертвый". Формы никуда не отправляются, корзина не работает, меню статичное (вшито в код).

Цель (Что мы делаем и ЗАЧЕМ):
Наша цель — вдохнуть жизнь во "внутрянку" моего нового сайта (2_NEW_SITE_DESIGN), не меняя его дизайн.

Мы ПОЛНОСТЬЮ ИГНОРИРУЕМ дизайн старого сайта. Мы берем принципы и логику из старого PHP-бэкенда и переписываем их на TypeScript в виде API и новой Админ-панели.

Итоговый продукт должен:

Выглядеть точь-в-точь как мой готовый 2_NEW_SITE_DESIGN.

Функционировать:

Бронирование: Форма бронирования стола должна отправлять уведомление в Telegram.

Заказ еды: Готовая корзина должна позволять оформить заказ, который тоже улетает в Telegram.

Меню: Меню на сайте должно загружаться динамически из базы данных, а не быть вшито в HTML.

Управляться: У сайта должна появиться новая, удобная админка (на /admin), в которую можно войти (admin / mk509918) и легко редактировать меню (добавлять/менять блюда и категории), чтобы не лезть в код.

Ниже приведен подробный технический план для достижения этой цели.

🚀 Итоговый План (Исправленная версия)
1. КЛЮЧЕВЫЕ СЕКРЕТЫ (Для переменных окружения):

TELEGRAM_BOT_TOKEN: 7874332998:AAEjVN5tyTSQtvAtRWQv296Xd3h95snX1oE

TELEGRAM_CHAT_ID: 1637641074

ADMIN_USER: admin

ADMIN_PASS: mk509918 (будет храниться в зашифрованном виде)

DATABASE_URL: (Получим от Vercel Postgres на Этапе 4)

🗄️ ЭТАП 1: База Данных и Динамическое Меню
Задача: "Отвязать" твое готовое меню от HTML и заставить его загружаться из базы данных.

Создать Базу Данных:

Создать новую базу Vercel Postgres.

Получить DATABASE_URL и добавить в секреты.

Настроить Prisma (ORM):

Настроить Prisma в проекте 2_NEW_SITE_DESIGN.

Создать в schema.prisma модели, основанные на старом .sql файле:

Category (Категория, напр. "Супы", "Напитки")

Product (Блюдо, с ценой, фото, описанием и связью с Category)

Выполнить prisma migrate dev для создания этих таблиц.

Перенести Данные (Seed):

Написать одноразовый скрипт (seed.ts), который прочитает старый pokepao.sql и, используя Prisma, заполнит новые таблицы Category и Product старыми данными (названиями блюд, ценами и т.д.).

"Оживить" Меню:

Создать API-роут /api/menu, который берет данные из новой БД (через Prisma) и отдает их.

Изменить твой готовый React-компонент меню: убрать из него "вшитые" блюда и заставить его делать fetch (запрос) на /api/menu и отрисовывать полученные данные.

🔌 ЭТАП 2: Подключение "Внутрянки" (Формы и Корзина)
Задача: Подключить твои существующие формы к "мозгам" (API).

API для Бронирования Стола:

Создать API-роут /api/reservations.

Написать TypeScript-сервис, который берет POST данные (имя, телефон, дата...) и отправляет их в Telegram (используя секреты TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID).

Подключить: Изменить твой готовый компонент формы бронирования, чтобы при onSubmit он отправлял fetch (POST) запрос на /api/reservations.

API для Заказа Еды (Корзина):

Создать API-роут /api/orders.

Написать TypeScript-сервис, который берет POST данные (данные клиента + массив блюд) и также отправляет их в Telegram.

Подключить: Изменить твою готовую страницу оформления заказа, чтобы при onSubmit она собирала данные из корзины и отправляла fetch (POST) запрос на /api/orders.

👑 ЭТАП 3: Создание Админ-Панели (Только Backend + Админский Frontend)
Задача: Создать с нуля удобную, защищенную админку на /admin для управления сайтом, взяв принцип из старой PHP-админки.

Создать Админский Frontend (Новые React-компоненты):

Создать новую папку (admin) в app (или pages).

Создать страницу входа /admin/login.

Создать защищенные страницы (дашборд).

Аутентификация:

Настроить NextAuth.js (или аналог).

Создать API-роут /api/auth/[...nextauth], который будет проверять, что логин (ADMIN_USER) и пароль (ADMIN_PASS) из секретов совпадают.

API для Админки (CRUD):

Создать защищенные API-роуты, которые может вызывать только админ (с проверкой сессии).

/api/admin/menu/categories (GET, POST, PUT, DELETE)

/api/admin/menu/products (GET, POST, PUT, DELETE)

/api/admin/gallery (GET, POST, DELETE)

Frontend Админки (Главная цель):

Создать на /admin/menu интерфейс, который использует API из пункта 3.

Важно: Интерфейс должен быть удобным для владельца:

Показывать список категорий ("Супы", "Напитки").

Позволять "Создать новую категорию".

При клике на категорию, показывать список блюд в ней.

Позволять "Добавить новое блюдо" (с формой: название, цена, описание, загрузка фото) в эту категорию.

Позволять редактировать и удалять блюда и категории.

Создать /admin/gallery для управления галереей.

Создать /admin/settings для управлен
ЕХНИЧЕСКОЕ ЗАДАНИЕ ДЛЯ РАЗРАБОТЧИКА
❌ 1. ЧТО НЕ НУЖНО ДЕЛАТЬ

Мне абсолютно не важно, работает ли приложение на Replit.
Можешь даже не пытаться его там запускать.

Replit — не продакшн.
Это только песочница, и меня не интересует его работа там.

✅ 2. ЧТО НУЖНО СДЕЛАТЬ И ЧТО Я ОТ ТЕБЯ ЖДУ
Главная цель:

Полностью подготовить приложение к деплою на RailVale и убедиться, что оно там запускается и работает без единой ошибки.

🔒 Уже готово с моей стороны:

На RailVale уже загружено и настроено:

Telegram bot

Bot token

Bot ID

User chat IDs

Все environment variables

Все секреты и токены

То есть ничего от Replit брать не нужно.
Всё уже лежит на RailVale.

🚀 3. ЧТО ТЕБЕ НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС
A) Полностью подготовить проект к запуску на RailVale

То есть:

проверить Dockerfile, start.sh, package.json, vite и build

проверить backend (Express)

проверить API пути

проверить работу Websocket’ов (если используются)

проверить работу Telegram уведомлений

убедиться, что все env-переменные корректно читаются

B) Убедиться, что база данных (PostgreSQL) на RailVale соответствует новой схеме

Проверить:

что таблицы создались

что поле extraPrice существует в ingredients и snapshotIngredients

что миграции прошли полностью

что всё совпадает с schema.ts

C) Проверить, что весь функционал запускается на проде

Тебе нужно локально собрать проект → затем деплоить на RailVale → проверить:

загрузку меню

сборку Wunschbowl

расчёт всех цен через pricingService

работу корзины

оформление заказа

отправку заказа в Telegram

расчёт total price

восстановление снапшотов

работу админки

работу галереи

D) Проверить каждую фотографию в проекте

Очень важно:

🔸 1. У каждого ингредиента должна быть фотография
🔸 2. У каждого экстра-ингредиента — тоже
🔸 3. У каждого блюда — тоже
🔸 4. На каждой странице меню должны отображаться правильные картинки
🔸 5. Все изображения должны корректно грузиться на продакшене

Если файла нет → должен быть fallback.

E) Проверить привязку изображений

Работает ли:

сопоставление изображений по имени ингредиента/блюда

загрузка /media/...

наличие файлов в продовой сборке

F) Убедиться, что Telegram уведомления работают именно на RailVale

То есть:

приходят ли уведомления

правильно ли отображаются ингредиенты

отображаются ли extraPrice

правильная ли итоговая сумма

форматирование

G) Всё должно работать быстро и стабильно

Приложение должно:

быстро собираться

запускаться без ошибок

отдавать страницы максимально быстро

не иметь broken images

не иметь undefined полей

не падать ни на одной операции

🧨 4. ИТОГ

Тебе не нужно запускать Replit.
Тебе нужно гарантировать 100% рабочий, полностью протестированный деплой на RailVale.
Ты — ИИ-разработчик, который работает над существующим проектом на Node.js + PostgreSQL. Ни при каких обстоятельствах не предлагай менять стек, не переписывай проект на другие технологии, не изменяй логическую структуру и не переделывай архитектуру. Твоя задача — упорядочить, нормализовать и довести до идеала текущий проект, сохранив всё, что уже работает. Не переписывай ничего с нуля, а только структурируй, вычищай, дополняй и логично организуй. Важно: не ломай фронт, не ломай существующий функционал, не меняй ценообразование Wunsch Bowl, не меняй формулы расчётов, не меняй структуру данных, если это приведёт к несовместимости с существующей логикой. Везде, где есть сомнения — фиксируй и выноси в отдельный список “на согласование”, а не додумывай за пользователя.

ПРИНЦИПЫ, КОТОРЫЕ НЕЛЬЗЯ НАРУШАТЬ:

Стек остаётся: Node.js + PostgreSQL.

Код не переписывать на другие языки или фреймворки.

Никаких предположений, если уверенность ниже 100%.

Если файл, изображение или сущность вызывают сомнение — занести в список для проверки.

Не ломать Wunsch Bowl: логика, структура, цены, формулы, связи — всё оставить как есть.

Никаких хардкод-данных: всё должно быть перенесено в БД.

Все изображения должны быть правильно переименованы, переложены в нужные папки, и привязаны к соответствующим блюдам или ингредиентам только по точному совпадению.

Если что-то сейчас работает на фронте — это должно продолжить работать.

ТВОЯ РАБОТА ВЫПОЛНЯЕТСЯ В 5 ЭТАПОВ:

ЭТАП 1 — ПОЛНАЯ ИНВЕНТАРИЗАЦИЯ ПРОЕКТА.
Сканируешь весь проект: backend, frontend, папки public, media, uploads, все изображения, все данные меню, все ингредиенты, все категории. Каждый найденный объект (изображение, категория, блюдо, ингредиент) фиксируешь в собственном техническом манифесте. Создаёшь единый JSON-манифест изображений: путь, текущий файл, предполагаемое назначение, уверенность. Если название файла содержит dish-slug → связываешь со строкой в БД или в menu.ts/ingredients.ts. Если уверенность < 100% — заносишь в отдельный раздел “Требует ручной проверки”. Не делай никаких догадок — если непонятно, так и пиши.

ЭТАП 2 — НОРМАЛИЗАЦИЯ ИЗОБРАЖЕНИЙ И ПАПОК.
Переносишь изображения в новую аккуратную структуру: /public/media/categories/{category_slug}/items/{item_slug}.webp — для блюд. Для ингредиентов: /public/media/ingredients/{тип}/{ingredient_slug}.webp. Переименовывай только на основе 100% соответствия slug блюду или ингредиенту. При обнаружении опечаток в названиях (например, oasted beetroot.png) — исправляешь их в корректный slug, но фиксируешь в отчёте. Дубликаты удаляешь только если их назначение абсолютно идентично. Если сомнения — переносишь оба файла, но помечаешь один как “дубликат на согласование”. Никаких автоматических догадок: только точное сопоставление.

ЭТАП 3 — МИГРАЦИЯ ВСЕХ ЗАХАРДКОЖЕННЫХ ДАННЫХ В БАЗУ.
Переносишь ВСЕ данные из server/data/menu.ts и server/data/ingredients.ts в PostgreSQL. Поскольку хардкод огромный, твоя задача — создать чёткую, однозначную и полную структуру записей в БД, строго соответствующую тому, что работало на фронтенде до миграции. Ничего не менять, только перенести. Никаких изменений цен, названий, категорий, наличия, slug полей, никаких новых характеристик — только корректная миграция. По окончании миграции создаёшь тех-отчёт: сколько блюд перенесено, сколько ингредиентов, сколько совпадений, сколько элементов потребуют ручной проверки.

ЭТАП 4 — РЕФАКТОРИНГ АДМИН-ПАНЕЛИ.
Добавляешь функционал так, чтобы теперь все операции CRUD работали напрямую с БД. Все категории, блюда, ингредиенты, изображения должны управляться только через админку. Логика должна быть прозрачной:
— создаётся категория → автоматически создаётся папка /media/categories/slug/items/
— создаётся блюдо → его изображение сохраняется строго по slug в правильную папку
— редактирование блюда → фронт обновляет данные без перезагрузки
Не меняй дизайн, не меняй структуру компонентов, не ломай существующие элементы — улучшай только то, что относится к управлению данными.

ЭТАП 5 — КОНТРОЛЬ КОРРЕКТНОСТИ WUNSCH BOWL.
Анализируешь текущее состояние калькулятора, но не меняешь его логику. Важно: формулы, цены, модификаторы, селекторы, extra-опции — всё оставить как есть. Единственное, что ты делаешь — переносишь исходные данные в БД и обеспечиваешь, чтобы калькулятор теперь читал их из базы, но математическую логику не трогал. Все ингредиенты и подкатегории должны быть корректно связаны. Если что-то не подходит под единую схему — фиксируй это как отдельную особенность, но не переписывай.

ФИНАЛЬНЫЕ ТРЕБОВАНИЯ:
— Создай manifest.json со всеми путями до/после.
— Создай migration-log.md со всеми шагами, что было перенесено.
— Создай images-review.json со списком неуверенных соответствий.
— Обеспечь, чтобы после завершения всех этапов фронт функционировал полностью без ошибок.
— Не меняй ни одного рабочего поведения без крайней необходимости.
— Всё должно быть организовано так, чтобы дальнейшее расширение меню, категорий, запросов к БД и загрузки изображений происходило без хаоса и без путаницы.

Если в процессе любой шаг будет вызывать сомнение — обязательно останавливаешься и добавляешь этот момент в список “неоднозначных”, а не делаешь предположений.
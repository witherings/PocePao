🚀 Итоговый План: Миграция сайта "PokePao" с PHP на TypeScript
1. КОНТЕКСТ ПРОЕКТА:

ИСТОЧНИК (Старый сайт): 1_OLD_SITE_BACKEND

Стек: PHP, MySQL, старая админка.

Функционал: Telegram-бот для бронирования стола, админ-панель (для управления меню, галереей, SEO, контентом).

Чего НЕТ: Корзины / Системы заказа еды.

ЦЕЛЬ (Новый сайт): 2_NEW_SITE_DESIGN

Стек: TypeScript, Next.js, React (вероятно, хостинг Vercel).

Дизайн: Новый, современный дизайн уже готов.

ГЛАВНАЯ ЗАДАЧА: "Перевести" всю ценную "внутрянку" (логику) со старого PHP-сайта на новый стек TypeScript. Мы не используем PHP. Мы переписываем функционал, интегрируем его с новым дизайном и переносим старые данные (меню, галерею) в новую, современную базу данных.

2. 🔑 КЛЮЧЕВЫЕ ДАННЫЕ И СЕКРЕТЫ (Для переменных окружения):

Telegram Bot Token: 7874332998:AAEjVN5tyTSQtvAtRWQv296Xd3h95snX1oE

Telegram Chat ID: 1637641074

Логин админа (желаемый): admin

Пароль админа (желаемый): mk509918 (Примечание: пароль должен храниться в переменных окружения (secrets) в зашифрованном виде (bcrypt), а не в открытом тексте).

📋 ЭТАП 1: Telegram-бот для БРОНИРОВАНИЯ СТОЛА (Перенос логики)
Задача: Воссоздать на TypeScript логику из 1_OLD_SITE_BACKEND/www/template/content/index.php (строки 122-162).

Создать API-роут в проекте 2_NEW_SITE_DESIGN (например, /api/reservations).

Настроить роут так, чтобы он принимал POST запрос со следующими JSON-полями:

fio (Имя и фамилия)

persons (Количество персон)

date (Дата)

time (Время)

phone (Телефон)

Добавить Token и Chat ID в переменные окружения (Secrets) в Replit/Vercel.

Написать TypeScript-сервис, который при вызове /api/reservations:

Читает секреты (Token/Chat ID).

Форматирует полученные данные в текстовое сообщение.

Отправляет это сообщение в Telegram через API.

Интегрировать Frontend: Подключить React-компонент формы бронирования из нового дизайна, чтобы он отправлял fetch или axios запрос на /api/reservations.

🛒 ЭТАП 2: Система ЗАКАЗА ЕДЫ / КОРЗИНА (Создание с нуля)
Задача: Реализовать полноценную систему заказа еды, которой не было в старом сайте.

Frontend (React):

Создать React-компоненты для "Корзины" (Cart).

Реализовать логику useContext или zustand для управления состоянием корзины (добавление/удаление блюд, подсчет суммы).

Создать страницу оформления заказа (checkout).

Backend (API):

Создать новый API-роут (например, /api/orders).

Логика API-роута /api/orders:

Принимает POST запрос (данные клиента + массив блюд в корзине).

Уведомление: Отправляет отформатированное сообщение о новом заказе в тот же Telegram-бот (используя те же секреты).

Сохранение: Сохраняет этот заказ в новой базе данных (см. Этап 4).

🛠️ ЭТАП 3: Воссоздание Админ-Панели на React
Задача: С нуля переписать на React/Next.js весь функционал старой PHP-админки из /www/admin.

Аутентификация (Login):

Создать защищенную группу роутов (например, /admin/**).

Создать страницу логина /admin/login.

Настроить систему аутентификации (например, NextAuth.js), которая проверяет логин/пароль (admin / mk509918) из переменных окружения или из таблицы admin_users в новой БД.

CRUD для "Меню" (Products):

Создать страницу /admin/menu.

Реализовать полный CRUD (Create, Read, Update, Delete) для блюд, который работает с новой базой данных (см. Этап 4).

Функции: "Создать блюдо", "Редактировать" (название, цена, описание, категория), "Удалить".

Реализовать загрузку/замену фотографий для блюд.

Просмотр Заказов и Броней:

Создать страницу /admin/orders, которая читает данные из новой БД и показывает список всех новых заказов (из Этапа 2) и бронирований (из Этапа 1).

Управление Галереей:

Создать страницу /admin/gallery (CRUD для фотографий).

Управление Контентом Сайта:

Создать страницу /admin/settings для редактирования:

SEO: Мета-теги (Title, Description) для страниц.

Тексты: Статические тексты ("О нас", "Контакты").

Ссылки: URL для соцсетей (WhatsApp, Instagram, TikTok).

Аналитика: Поле для вставки кода Google Analytics.

Баннеры: Управление слайдером на главной (если это нужно в новом дизайне).

🗄️ ЭТАП 4: Миграция Базы Данных (MySQL → Postgres)
Задача: Перенести все данные (меню, галерею) из старого файла pokepao.sql (MySQL) в новую базу данных, совместимую с хостингом Vercel.

Объяснение процесса: Мы не можем использовать MySQL на Vercel. Поэтому мы создадим новую базу данных Vercel Postgres. Старый .sql (MySQL) и новый Postgres несовместимы напрямую. Мы не будем "заливать" старый файл. Мы воссоздадим структуру в новой БД, а затем перенесем в нее только чистые данные (названия блюд, цены и т.д.). Новый TypeScript-код будет "общаться" с этой новой БД через Prisma (это современный инструмент, который заменяет старые PHP-запросы к MySQL).

Шаги по реализации:

Создать новую базу данных Vercel Postgres в настройках проекта Vercel.

Получить строку подключения (CONNECTION_STRING) и добавить ее в переменные окружения (Secrets).

Настроить Prisma в проекте 2_NEW_SITE_DESIGN.

Определить Prisma-схемы (модели) для новых таблиц. Они будут основаны на старых, но адаптированы:

User (для админов)

Product (блюда)

Category (категории меню)

GalleryImage (фото для галереи)

Order (для Этапа 2)

Reservation (для Этапа 1)

Выполнить миграцию Prisma (prisma migrate dev), чтобы создать эти таблицы в новой базе Vercel Postgres.

Перенос данных:

Написать одноразовый скрипт (seed.ts).

Этот скрипт должен прочитать данные из старого .sql файла (или его JSON-версии) и, используя Prisma, создать записи (prisma.product.createMany(...)) в новой базе данных.

Это перенесет все старое меню и галерею в новый сайт.